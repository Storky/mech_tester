import { fade, makeStyles } from '@material-ui/core/styles';

export const usePageWrapperStyles = makeStyles({
    root: {
        paddingTop: 20,
        paddingBottom: 20,
    },
    h: {
        marginBottom: 20,
    }
});
